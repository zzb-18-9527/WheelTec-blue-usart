# 二维云台远控程序 - 实现计划

## 概述

基于 PyQt5 + bleak (BLE) 构建 PC 端 EXE 应用，通过 BLE 串口透传控制 F32C 二自由度云台（两个无刷电机），实现使能/失能、回零、角度设置、机械角度查询及复杂组合运动。

---

## 技术选型

| 层面 | 技术 | 理由 |
|------|------|------|
| GUI | PyQt5 | 成熟稳定，组件丰富，PyInstaller 打包方便 |
| BLE通信 | bleak | 跨平台 BLE 库，支持 Windows 10+，异步 API |
| 串口备选 | pyserial | 当用户使用串口蓝牙模块时作为备选 |
| 打包 | PyInstaller | 一键打包为 Windows EXE |
| 异步 | QThread + asyncio | BLE 异步操作与 GUI 主线程分离 |

---

## 通信协议总结（从手册提取）

### 云台电机地址
- X轴电机 (01): `01`
- Y轴电机 (02): `02`

### 协议帧格式
`帧头(7A) + 地址 + 功能码 + [数据] + BCC校验 + 帧尾(7B)`

### 关键指令（HEX）

| 功能 | 电机01 (X轴) | 电机02 (Y轴) |
|------|-------------|-------------|
| 使能 | 7A 01 06 7D 7B | 7A 02 06 7E 7B |
| 失能 | 7A 01 05 7C 7B | 7A 02 05 7D 7B |
| 单圈绝对(直通)模式 | 7A 01 00 00 04 7C 7B | 7A 02 00 00 04 7C 7B |
| 单圈绝对(T型)模式 | 7A 01 00 00 02 7A 7B | 7A 02 00 00 02 7A 7B |
| 速度控制 | 7A 01 01 XX XX BCC 7B | 7A 02 01 XX XX BCC 7B |
| 切换速度模式 | 7A 01 00 00 00 78 7B | 7A 02 00 00 00 78 7B |
| 单圈绝对角度控制 | 7A 01 03 XX XX BCC 7B | 7A 02 03 XX XX BCC 7B |
| 多圈绝对角度控制 | 7A 01 02 X1X1X2X2 BCC 7B | 7A 02 02 X1X1X2X2 BCC 7B |
| 请求机械角度 | 7A 01 0E 02 73 7B | 7A 02 0E 02 74 7B |
| **设置单圈绝对0点** | **7A 01 0A 71 7B** | 7A 02 0A 72 7B |

### BCC校验算法
对 帧头+地址+功能码+所有数据字节 逐字节 XOR。

### 响应格式
`7A + 地址 + 功能码类型 + 高16位 + 低16位 + BCC + 7B`
- 机械角度反馈: 功能码=02，数据为角度*10（如0x05DC = 1500 → 150.0°）

### 单圈绝对角度
范围 0~359.9°，放大10倍发送。如150° → 0x05DC → 发送 7A XX 03 05 DC BCC 7B

### 速度值
范围 0~1000 RPM。正数=正转，负数=反转（补码）。

---

## 架构设计

### 模块划分

```
gimbal_controller/
├── main.py                  # 入口，启动QApplication
├── connection/
│   ├── __init__.py
│   ├── ble_manager.py       # BLE扫描、连接、收发（基于bleak）
│   └── serial_manager.py    # 串口备选连接（基于pyserial）
├── protocol/
│   ├── __init__.py
│   ├── commands.py          # 电机指令生成（所有HEX帧构造）
│   ├── parser.py            # 电机响应解析（角度、速度反馈）
│   └── bcc.py               # BCC校验算法
├── state/
│   ├── __init__.py
│   └── motor_state.py       # 两电机状态管理（角度、使能、模式、零点状态）
├── ui/
│   ├── __init__.py
│   ├── main_window.py       # 主窗口布局与控件
│   ├── connect_dialog.py    # BLE设备扫描与连接对话框
│   ├── hex_panel.py         # 手动HEX发送面板（调试用）
│   └── styles.py            # QSS样式定义
└── worker/
    ├── __init__.py
    └── sequence_worker.py   # 复杂运动序列后台线程执行器
```

### 数据流

```
用户操作 → UI层 → Worker线程 → Protocol层(构造HEX) → Connection层(BLE发送)
                                                                      ↓
UI层(更新显示) ← MotorState ← Parser层(解析响应) ← Connection层(BLE接收)
```

### 关键设计决策

1. **BLE与串口双模式**: 主推BLE(bleak)，同时支持串口(pyserial)，连接对话框提供切换选项。先扫描BLE设备，若无法BLE连接则回退串口。
2. **异步通信**: BLE操作全部通过 QThread + asyncio 运行，避免阻塞 GUI 主线程。
3. **指令发送间隔**: 电机间指令间隔 ≥ 1ms（协议要求），复杂序列中使用 QTimer 控制节奏。
4. **可扩展协议层**: `commands.py` 封装为工厂函数，新增指令只需添加函数，不影响 UI。
5. **运动序列可配置**: `sequence_worker.py` 用状态机实现，每个动作步可独立配置角度、速度、方向。

---

## GUI 主窗口布局

```
┌─────────────────────────────────────────────────────────────────┐
│  [连接状态: 已连接 BLE-XX]  [连接] [断开]                          │
├────────────────┬──────────────────────┬────────────────────────┤
│  连接控制区     │   电机状态显示区        │   动作控制区             │
│                │                      │                        │
│ [使能/失能]    │  01电机角度: 120.5°   │  [回零]                │
│ 自锁开关       │  02电机角度: 45.0°    │  [获取机械角度]         │
│                │                      │                        │
│               │  基准角度:             │  设置01角度: [___]°    │
│               │  01基准: 120.5°       │  [确认旋转]            │
│               │  02基准: 45.0°        │  [保存角度]            │
├────────────────┴──────────────────────┴────────────────────────┤
│  X轴点动控制                                                    │
│  速度: [___]RPM  [X轴顺时针旋转]  [X轴逆时针旋转]                │
│  (按下旋转，松开停止)                                            │
├────────────────────────────────────────────────────────────────┤
│  运动序列控制                                                   │
│  偏转角度: [___]°   速度: [___]RPM   [执行运动序列]  [停止]     │
├────────────────────────────────────────────────────────────────┤
│  手动HEX面板 (可折叠)                                           │
│  发送: [          ] [发送]    接收日志: [...]                     │
└────────────────────────────────────────────────────────────────┘
```

### X轴点动控制

**"X轴顺时针旋转"按钮**:
- 按下(mousedown): 切换01电机为速度模式，发送速度指令(正值=顺时针)
- 松开(mouseup): 发送速度0指令，电机停止

**"X轴逆时针旋转"按钮**:
- 按下(mousedown): 切换01电机为速度模式，发送速度指令(负值=逆时针)
- 松开(mouseup): 发送速度0指令，电机停止

**速度值**: 用户在点动区域的速度输入框中设定(范围1~1000 RPM)

**实现方式**: 使用QPushButton的`pressed`和`released`信号实现按下/松开检测。每次按下时先发送控制模式切换指令(速度模式 `7A XX 00 00 00 BCC 7B`)，再发送速度指令。

### "保存角度"按钮行为
- 点击后 **向01电机发送设置单圈绝对0点指令** (`7A 01 0A 71 7B`)
- 该指令将01电机当前的机械位置设为其新的0°参考点（可掉电保存）
- **不向02电机发送任何指令**，02电机的机械0点不受影响
- 发送成功后，界面上记录并显示保存时的机械角度值作为参考
- 若未保存就点击"执行运动序列"，弹出提示要求先保存

---

## 运动序列详细逻辑

### 前置准备

1. 用户通过"设置01角度"功能将01电机旋转到指定角度
2. 用户点击 **"保存角度"按钮** → 向01电机发送设置零点指令 (`7A 01 0A 71 7B`)
   - 01电机当前机械位置被重设为其新的0°参考点（可掉电保存）
   - 02电机保持不变（不发送任何指令）
3. 此后01电机的所有角度控制均以新0°为基准

### 执行流程

```
前置: 01电机零点已被重设（保存角度按钮），02电机机械0点已校准为竖直

用户设定: offsetAngle (偏转角度), speed (速度 RPM)

// === 阶段1: 单轴运动 (共4步，每步回零后进入下一步) ===
1. 02保持0°不动, 01顺时针偏转至 (0 + offsetAngle)° → 01回零(0°)
2. 02保持0°不动, 01逆时针偏转至 (360 - offsetAngle)° → 01回零(0°)
3. 01回到0°保持不动, 02顺时针偏转至 (0 + offsetAngle)° → 02回零(0°)
4. 01回到0°保持不动, 02逆时针偏转至 (360 - offsetAngle)° → 02回零(0°)

// === 阶段2: 组合运动 (共4步，每步双回零后进入下一步) ===
5. 01顺(0+offset) + 02顺(0+offset) → 双回零(0°, 0°)
6. 01顺(0+offset) + 02逆(360-offset) → 双回零(0°, 0°)
7. 01逆(360-offset) + 02顺(0+offset) → 双回零(0°, 0°)
8. 01逆(360-offset) + 02逆(360-offset) → 双回零(0°, 0°)
```

**说明**:
- 01电机: 偏转基于"保存角度"后的新0°
- 02电机: 偏转基于其已校准的机械0°（竖直位置）
- "保持不动"= 不发送角度指令，电机维持当前位置（使能状态下有力矩保持）

### 单步执行细节
1. 设置两电机为单圈绝对模式(直通)
2. 发送速度指令(需要运动的电机)
3. 发送目标角度指令(需要运动的电机；保持不动的电机维持当前角度)
4. 等待电机到达目标位置（轮询机械角度 或 固定等待时间）
5. 发送回零角度指令(回到对应base值)
6. 等待回零完成
7. 进入下一步

### 角度计算
两电机偏转均基于各自0°参考点:
- 01电机顺偏转 = (0 + offsetAngle)°，逆偏转 = (360 - offsetAngle)°，回零 = 0°
- 02电机顺偏转 = (0 + offsetAngle)°，逆偏转 = (360 - offsetAngle)°，回零 = 0°
- 两个0°含义不同: 01的0°=保存角度时的位置，02的0°=已校准的机械竖直位

---

## 实现步骤（建议顺序）

### 阶段 1: 基础通信框架 (核心)
1. 创建项目结构
2. 实现 `bcc.py` - BCC校验算法
3. 实现 `commands.py` - 所有电机指令生成函数
4. 实现 `parser.py` - 电机响应解析
5. 实现 `ble_manager.py` - BLE扫描、连接、收发
6. 实现 `serial_manager.py` - 串口连接备选
7. 实现 `connect_dialog.py` - 连接对话框

### 阶段 2: 主界面与基本控制
8. 实现 `main_window.py` - 主窗口布局
9. 实现 `motor_state.py` - 电机状态管理（角度、使能、模式、零点状态）
10. 实现使能/失能自锁按钮
11. 实现回零按钮
12. 实现角度设置与旋转
13. 实现获取机械角度按钮
14. **实现"保存角度"按钮** - 向01发送设置零点指令 (`7A 01 0A 71 7B`)
15. **实现X轴点动控制** - 顺时针/逆时针旋转按钮（pressed→速度旋转，released→停止）
16. 实现手动HEX面板

### 阶段 3: 复杂运动序列
16. 实现 `sequence_worker.py` - 运动序列状态机
17. 实现阶段1单轴偏转+回零（01顺/逆、02顺/逆，共4步）
18. 实现阶段2组合偏转+回零（顺顺/顺逆/逆顺/逆逆，共4步）
19. 实现停止/中断功能

### 阶段 4: 打包与优化
19. PyInstaller 打包为 EXE
20. 调试 BLE 连接稳定性
21. UI 美化与交互优化

---

## BLE 连接特殊处理

由于 BLE UUID 需扫描确认，应用设计为:

1. **扫描阶段**: 调用 `bleak.BleakScanner.discover()` 列出附近 BLE 设备
2. **用户选择**: 弹出设备列表，用户选择云台蓝牙模块
3. **服务发现**: 连接后扫描所有 GATT Services/Characteristics
4. **自动检测 NUS**: 优先查找 Nordic UART Service (UUID: `6E400001-...`)，若找到则自动使用其 Write Characteristic
5. **手动指定**: 若未找到 NUS，展示所有可写特征让用户选择
6. **回退串口**: 若 BLE 扫描不到设备，提供串口手动输入选项

---

## 验证方案

1. **协议验证**: 用串口工具手动发送手册中的示例指令，验证电机响应
2. **BLE连接测试**: 连接蓝牙模块后，手动HEX面板发送使能指令，观察LED状态变化
3. **基本控制测试**: 按下使能/失能按钮，确认电机响应正确
4. **角度控制测试**: 设置不同角度值，验证电机转动到正确位置
5. **运动序列测试**: 以低速度小角度执行完整运动序列，验证每步动作
6. **EXE打包测试**: 打包后在干净 Windows 机器上运行测试

---

## 依赖清单

```
PyQt5>=5.15
bleak>=0.21
pyserial>=3.5
pyinstaller>=6.0  (打包时)
```
